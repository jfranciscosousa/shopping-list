# Created and maintained by `pnpm setup:local`; do not commit `.env.local`.
DATABASE_URL="postgresql://postgres@127.0.0.1:PORT/shopping_list_local_WORKSPACE"
MIGRATION_DATABASE_URL="postgresql://postgres@127.0.0.1:PORT/shopping_list_local_WORKSPACE"

# This only signs local sessions. It is not a production secret.
SECRET_KEY_BASE="local-development-secret"
ALLOWED_DEV_ORIGINS="localhost,127.0.0.1"

# Optional. `pnpm ai:env:pull` writes this value to `.env.local`.
# VERCEL_OIDC_TOKEN=""

# Optional: restrict local registration.
# INVITE_TOKEN=""
